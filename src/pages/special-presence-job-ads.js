import React from 'react'
import Layout from '../components/layout' // If you want to wrap it with Layout
import SpecialPresence from '../components/specialpresence'

const SpecialPresences = () => {
  return (
    <Layout>
        <SpecialPresence />
    </Layout>
  )
}

export default SpecialPresences
